{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "versionup:rc": "lerna version --conventional-commits --create-release github --conventional-prerelease --preid next --yes",
    "versionup:prod": "lerna version --conventional-commits --create-release github --conventional-graduate --yes",
    "publish:packages": "lerna publish from-git --yes"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
