{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "versionup:prod": "lerna version --conventional-commits --create-release github --yes",
    "publish:packages": "lerna publish from-git --yes",
    "versionup:canary": "lerna version --conventional-commits --no-push --yes",
    "publish:canary":"lerna publish --loglevel=verbose --canary --exact --preid next --force-publish --yes"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
